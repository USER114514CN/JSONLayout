# alpha 1.0
现在可以搭建最基础的布局了，但是不支持Panel的contents属性和监听器，后续会补全

# alpha 1.0.1
更新了 Panel 的 contents 属性，现在可以写嵌套了

# alpha 1.0.2
可以通过像这样的方式加载public static常量
```json
{
  "class": "CONST",
  "name": "com.yourpackagename.YourClassName.YOUR_CONSTANT_NAME"
}
```
